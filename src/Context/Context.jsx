import { createContext } from "react";



const Context = () => {
    const ThemeContext = createContext();
    return (
        <div>
            
        </div>
    );
};

export default {Context, ThemeContext};